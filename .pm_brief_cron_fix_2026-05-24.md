# PM 발주문 — cron 사망 RC1+RC2 fix (2026-05-24)

> **받는 pane**: stock 세션 pane 1 (claude). 너가 직접 수행.
> **PM이 RC 확정 완료 후 발주**. 추측 fix 금지, 아래 방향대로만 진행.
> 완료 보고 시 PM이 L2 hunk + 산출물 직접 검증한다.

---

## 배경

PM `/hih-investigate`로 RC 정밀 조사 완료. cron 3종 중 2종 사망:
- **news collector kr/us**: 5/20 12:13 UTC 이후 90회 연속 ImportError
- **earn_reporter**: 5/24 처음 traceback (rows None TypeError)

(briefing APScheduler 데몬 정지는 별건 — Hermes 발주, 1-66 P1 task와 매핑)

---

## RC1 — sentiment collector ImportError (4일째 90회 실패)

### 원인 확정
- 커밋 `c233d81` (2026-05-12 17:55) "archive: 6개 휴면 collector 이동"이 다음 6개 파일을 `archive/collectors_dormant/`로 옮김:
  - aaii_collector.py
  - google_trends_collector.py
  - naver_community_collector.py
  - options_iv_collector.py
  - reddit_collector.py
  - stocktwits_collector.py
- 그런데 **`src/collectors/sentiment/__init__.py`에서 6개 import + `__all__` 6개 항목이 그대로 남음** → 첫 import (aaii)에서 `ModuleNotFoundError` → 전체 sentiment 패키지 로드 실패 → `from src.collectors.news import RSSNewsCollector`도 연쇄 실패

### 현재 살아있는 collector (이것만 유지)
- `cnn_fear_greed_collector.py` ✅
- `putcall_collector.py` ✅

### fix 방향
`src/collectors/sentiment/__init__.py` 편집:
1. 6개 깨진 import 라인 제거 (aaii / google_trends / naver_community / options_iv / reddit / stocktwits)
2. `__all__` 리스트에서 동일 6개 클래스명 제거
3. 살아있는 cnn_fear_greed + putcall 2개만 남기기

### 검증 (코드 변경 후 필수 실행)
```bash
cd /home/window11/stock
# 1. import 자체가 통과하는지
python3 -c "from src.collectors.sentiment import CNNFearGreedCollector, PutCallRatioCollector; print('sentiment OK')"
python3 -c "from src.collectors.news import RSSNewsCollector; print('news OK')"
# 2. 실제 수집 1회 수동 실행 (cron이랑 동일 명령)
cd /home/window11/stock && python3 scripts/collect_news.py --market kr 2>&1 | tail -10
cd /home/window11/stock && python3 scripts/collect_news.py --market us 2>&1 | tail -10
```
→ ✅ 모두 traceback 없고 "KR: N articles stored" 같은 정상 출력 확인

---

## RC2 — earn_reporter rows None TypeError (오늘부터)

### 원인 확정
- Nasdaq API 응답 형식 변경: `{"data": {"rows": null}}` 형태로 옴 (어제까지 빈 배열이었던 듯)
- `scripts/earn_reporter.py:238` `rows = data["data"].get("rows", [])` — 키가 존재하면 default `[]` 적용 안 됨 → `rows = None` → 240줄 `for row in None` → TypeError
- PM 실측: 5/23 날짜로 curl 호출해도 `rows type: NoneType` 확인됨

### fix 방향 (1줄)
`scripts/earn_reporter.py:238` 한 줄만 수정:
```python
# 변경 전
rows = data["data"].get("rows", [])
# 변경 후
rows = data["data"].get("rows") or []
```

다른 변경 하지 마. 로깅 추가나 알림 같은 거 다 보류 (사용자가 "최소 변경"으로 결정).

### 검증
```bash
cd /home/window11/stock
# 1. 어제 날짜로 수동 실행 (cron이랑 동일)
python3 scripts/earn_reporter.py --date 2026-05-23 2>&1 | tail -10
# 2. 빈 케이스 graceful exit 확인
echo $?  # 0이어야
```
→ ✅ traceback 없고 "Found 0 companies reporting" 같은 정상 로그

---

## 커밋 + PR

### 커밋 2개 분리 (RC별)
```
fix(collectors): sentiment __init__.py archive 후속 정리 (6 collector import 제거)

RC: c233d81 archive 작업 시 __init__.py 미정리 → ModuleNotFoundError 5/20 이후 90회 실패
영향: src/collectors/news 전체 import 실패 → collect_news.py kr/us cron 4일 정지
검증: import test + 수동 collect_news.py kr/us 실행 정상 출력
```

```
fix(earn_reporter): Nasdaq API rows None 응답 graceful 처리 (1줄)

RC: Nasdaq calendar API 응답 {"data": {"rows": null}} 변경 → 240줄 NoneType iter
영향: 매일 05:30 earn_reporter cron 100% 실패 (1-64 데이터 공급 중단)
검증: --date 2026-05-23 수동 실행 traceback 없음, exit 0
```

### PR 1개로 묶기
- 브랜치: `fix/cron-sentinel-import-and-earn-rows-guard`
- base: master
- title: `fix(cron): sentiment import cleanup + earn rows None guard (cron 2종 복구)`
- body: RC 2건 별도 섹션 + 검증 결과 명시

`/ship` 스킬 사용 권장.

---

## 작업 순서

1. 이 발주문 읽고 RC1+RC2 이해
2. brnach 생성 → RC1 fix → 검증 → 커밋
3. RC2 fix → 검증 → 커밋
4. push → PR (자동 머지 X, PM 검토 후 사용자 승인)
5. PR URL을 PM에 보고 + 검증 출력 그대로 첨부

---

## 금지 사항

- 다른 파일 리팩터링 X
- archive/ 안의 6 collector 복원하려고 시도 X (의도된 archive)
- RC2에 로깅/알림 추가 X (최소 변경 결정)
- master 직접 push X (PR 필수)
- 검증 실패 시 추측 재시도 X — PM에 보고 후 재지시 받기

---

**완료 보고 형식**:
```
## RC1+RC2 fix 완료
- RC1 커밋: <hash>
- RC2 커밋: <hash>
- PR: <URL>
- 검증 출력:
  - sentiment import: <붙여넣기>
  - collect_news kr/us: <붙여넣기>
  - earn_reporter --date 2026-05-23: <붙여넣기>
```

질문 있으면 발주 전에 PM에 물어. 추측으로 가지 마.
